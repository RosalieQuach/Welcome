<h2><%= @post.title %></h2>
<p><%= @post.description %></p>
<p><%= @post.is_offer %></p>

<%= link_to "Delete #{@post.title}",
            post_path(@post),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
